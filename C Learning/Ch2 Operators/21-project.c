#include <stdio.h>
int main()
{
    int a;
    // b = a;
    int b = a;
    return 0;
    int i = 2 ^ 2; // Its valid because ^ is bitwise xor oparetor but our answer will not b 2*2 i.e =4
    char dt = '2'; //'21';

    // what is the return data type 3.0/8-3 its not float more accurate is double
}
