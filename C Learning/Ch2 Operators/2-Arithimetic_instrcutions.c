#include <stdio.h>

int main()
{

    int a = 4, b = 6;
    printf("The values of a + b =%d\n", a + b);
    printf("The values of a - b =%d\n", a - b);
    printf("The values of a * b =%d\n", a * b);
    printf("The values of a / b =%d\n", a / b);
    return 0;
                             }
