#include <stdio.h>

int main()
{

    // int a = 4; // Type declaration instruction

    float b = a + 8.9; // Order is nesessary
    float a = 1.1;
    printf("The sum is =%f", b);
    return 0;
}
#include <stdio.h>
int main()
{
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    
}